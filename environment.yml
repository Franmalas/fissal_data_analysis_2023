# conda env create --file environment.yml
name: fissal_data_analysis_2023
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.12
  - pip:
    - pyhere
    